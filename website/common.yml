services:
  service-base:
    restart: on-failure:7
    environment:
      - TZ=${TZ:-Europe/Paris}
    security_opt:
      - no-new-privileges:true
    user: ${PUID:-1000}:${PGID:-1000}
    cap_drop:
      - SYS_ADMIN
    read_only: true
